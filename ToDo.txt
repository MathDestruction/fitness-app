[DONE] - Need to be able float values for time (Eg, 37.40) → Duration input now accepts decimals (step=0.01), detail view shows decimal values
[DONE] - Goals element still becoming vertical on my screen → Breakpoint lowered to 320px, added min-width:0 overflow protection to prevent grid blowout
[DONE] - On page refresh, info/data is lost with loading showing → Auth state handler now only resets on explicit SIGNED_OUT event; init error handling shows data instead of resetting to login
[DONE] - Site should also remember users Display name and theme preference once initially set → Already persists via localStorage per user, verified load order is correct
[DONE] - Any sessions logged on the same day should remain separate sessions in the database, but should all contribute to the same daily values → Weekly Sessions goal counts unique dates, all stats naturally sum across all sessions per date
[DONE] - On the goals element, the fastest 5km should be reached once i get under 30min → Progress ring now uses 60→30 min scale (0%→100%); ring goes green at ≤30 min
[DONE] - tool tips/info displayed after tapping on a graph or chart should remain for several seconds before fading, or in another part of the screen is tapped → Chart.js stickyTooltip plugin: tooltips persist ~4s on tap, dismissed on outside click
[DONE] - For the consistency visualizer, lets reduce the size considerably. It should be a short section (low height) but its width can span the width of the screen/block element → Heatmap cells reduced from 14px squares to 8px-tall rectangles with tighter gaps

